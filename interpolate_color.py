import numpy as np

def interpolate_color(x1, x2, x, Color1, Color2):
    color = np.zeros((1, 1, 3))
    # Do some stuff here
    return color


def color_pixel_gouraud(x, y, vertices, vcolors):
    color = np.zeros((1, 1, 3))
    return color;